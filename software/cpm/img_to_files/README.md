# Extract Files from CP/M Image
Given a CP/M disk image in ImageDisk (IMD) or Altair (DSK) format, this programme extracts the CP/M files and system data.

## Example commands:
python3 img_to_files.py cpm2.dsk
python3 img_to_files.py CPM22RED.IMD

